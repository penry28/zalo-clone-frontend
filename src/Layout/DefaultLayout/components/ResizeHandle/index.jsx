import { StyledResizeHandle } from './styled';

function ResizeHandle() {
    return (
        <StyledResizeHandle className="resize-handle">

        </StyledResizeHandle>
    );
}

export default ResizeHandle;
